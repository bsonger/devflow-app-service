# Platform Notes

This directory keeps small repo-local runtime notes for `devflow-app-service`.

Use it for:
- startup/runtime shape
- local implementation entrypoints
- service-specific platform notes that are still useful next to the code

Do not use it for:
- cross-repo ownership summaries
- control-layer flow docs
- duplicated API catalogs already documented in `../docs/`

Primary local note:
- `app-service/README.md`
