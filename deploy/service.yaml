apiVersion: v1
kind: Service
metadata:
  name: devflow-app-service
  namespace: devflow-staging
spec:
  selector:
    app.kubernetes.io/name: devflow-app-service
  ports:
    - name: http
      port: 8081
      targetPort: http
  type: ClusterIP
